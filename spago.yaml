workspace:
  packageSet:
    registry: 77.4.0
  extraPackages:
    # wasm-base, pinned to the fork's Int64 branch which adds `Wasm.Int64` (the v0.1.x tags predate it).
    # Pinned to a commit SHA for deterministic CI rather than a moving branch ref.
    wasm-base:
      git: "https://github.com/harryprayiv/purescript-wasm-base.git"
      ref: "58524ef125d9cb6aa70567571d95c39a60bf39d8"