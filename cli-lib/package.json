{
  "name": "cli-lib",
  "type": "module",
  "scripts": {
    "compile": "spago build -p cli-lib",
    "test:unit": "spago test -p cli-lib --monochrome",
    "test": "npm run test:unit",
    "format": "purs-tidy format-in-place src test",
    "check": "purs-tidy check src test"
  }
}