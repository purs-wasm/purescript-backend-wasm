/.spago
/output
/output-es
/output-wasm
/output-purwc
# ADR 0042/0040: the local content-addressed store the test suite / pre-push hook share via
# $PURS_WASM_STORE (so the library closure compiles once across test programs).
/.purs-wasm-store
/dist
# generated from runtime/runtime.wat by `npm run build:runtime` (chained into test:e2e)
/runtime/runtime.wasm
**/node_modules
**/.DS_Store
.psc-ide-port
.spec-results
.psci_modules
.purs-repl
# benchmark snapshots and rendered graphs: regenerated by bench/*.sh, no longer tracked
# (the CI Benchmark workflow renders them fresh and publishes to GitHub Pages)
/bench/snapshots
/bench/results
# bench build outputs (per-backend artifacts produced by the bench scripts)
/bench/output
/bench/output-js-es
/bench/output-wasm
/bench/output-wasm-noopt
/ulib/**/*.wasm
lib/

# CLI-driven e2e build artifacts (ADR 0031 phase 5)
compiler/test/e2e-build/
compiler/test/e2e-fixtures-out/

#
_draft
.claude-session

# purs-wasm npm bundle build artifacts
purs-wasm/bundle/
purs-wasm/.build/
purs-wasm/_build/
purs-wasm/_build-purwc/
# the bundled `purwc` worker prepack ships inside the package (NOT the top-level `purwc/` workspace)
purs-wasm/purwc/
purs-wasm/runtime/
purs-wasm/lib/
purs-wasm/*.tgz
purs-wasm/package/
purs-wasm/LICENSE

# nix build output
result
